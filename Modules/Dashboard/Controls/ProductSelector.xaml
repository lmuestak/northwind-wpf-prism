<UserControl x:Class="Northwind.Controls.ProductSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Northwind.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="260"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontWeight="SemiBold"
                   Foreground="{StaticResource PrimaryColorBrush}"
                   Text="Product" />
        <ComboBox Grid.Row="1"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  md:ComboBoxAssist.ShowSelectedItem="True"
                  md:HintAssist.Hint="Select a product"
                  md:HintAssist.HintOpacity=".26"
                  ItemsSource="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:ProductSelector}}, Path=Products}"
                  SelectedValue="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type local:ProductSelector}}, Path=Product}"
                  Style="{StaticResource MaterialDesignComboBox}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="5" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0"
                                   Grid.RowSpan="2"
                                   Grid.Column="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   Foreground="{StaticResource PrimaryColorBrush}"
                                   Text="{Binding ProductId}"
                                   TextAlignment="Center" />
                        <TextBlock Grid.Row="0"
                                   Grid.Column="2"
                                   Foreground="{StaticResource PrimaryColorBrush}"
                                   Text="{Binding ProductName, Converter={StaticResource TrimTextConverter}}" />
                        <TextBlock Grid.Row="1"
                                   Grid.Column="2"
                                   Margin="1,0,0,0"
                                   FontSize="10"
                                   Foreground="{StaticResource DarkGrayColorBrush}"
                                   Text="{Binding Path=CategoryWithQuantityPerUnit}" />

                    </Grid>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
